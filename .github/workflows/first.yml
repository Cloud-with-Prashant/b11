name: b11-workflow 

on: push 

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: create a file 
        run: echo "this is test reports" > report.txt 

  second-job:
    runs-on: ubuntu-latest
    needs: first-job
    steps:
      - name: viewing the reports from first job
        run: cat report.txt 

    
          

     

