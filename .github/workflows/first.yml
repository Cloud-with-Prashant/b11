name: b11-workflow 

on: push 

jobs:
  first:
    runs-on: ubuntu-latest
    steps:
      - name: print message 
        run: |
              echo "hello guys first stage "
              sleep 5
  second:
    runs-on: ubuntu-latest
    needs: first
    steps:
      - name: print message 
        run: | 
          echo "hello guys second stage"
          sleep 5 

